import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Lambda from './layouts/App';

ReactDOM.render(
  <React.StrictMode>
    <Lambda />
  </React.StrictMode>,
  document.getElementById('root')
);
